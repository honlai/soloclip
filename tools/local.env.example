# Copy to tools/local.env (gitignored).
# cron runs with a minimal PATH, so the environment that owns `soloclip` has to be
# named explicitly for the boot-time resume to work.
SOLOCLIP_ENV=/path/to/conda/envs/soloclip/bin
