# pyannote's models are gated: a token alone is not enough, the terms must also be
# accepted on the HuggingFace website. HF_TOKEN, this file, and the token left by
# `huggingface-cli login` are all checked, in that order.
HF_TOKEN=hf_your_token_here
